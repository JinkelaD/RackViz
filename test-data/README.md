# RackViz 导入测试数据

生成时间：2026-09-11　|　对应系统版本：v1.2.0（Stage 1 依赖跃迁后）

## 文件清单

| 文件 | 用途 |
|------|------|
| `RackViz-测试数据-100条.xlsx` | **主数据**：100 条设备记录，覆盖 4 个机房 / 12 个机柜 / 13 个型号 / 8 种设备类型 / 5 种高度，U 位零冲突。含 **16 列**（末列为设备类型） |
| `RackViz-测试数据-边界用例.xlsx` | **补充用例**：8 行边界数据，专测三重查重、字段容错，以及 N-21 的**英文枚举关联**与**未知类型值兜底** |
| `README.md` | 本文件 |

## 一、列契约（严格对齐 `src-tauri/src/excel.rs`）

导入路径**按列序号 0-14 读取，无表头识别**，第 0 行固定被当作表头跳过。文件表头仅作可读性用途，与导入逻辑无关。

| 列序 | 表头 | 导入语义 | 本数据取值 |
|:---:|------|---------|-----------|
| 0 | 设备名称 | `name`；为空则整行被 `continue` 丢弃 | `{机柜}-{角色}-{序号}`，如 `A-01-应用服务器-01` |
| 1 | 型号 | 非空则 `find_or_create_model` 自动建型号 | 13 个真实型号名 |
| 2 | 机房 | ⚠️ **被 `excel.rs:248` 的 `let _room_name` 丢弃**（v2.0.0 的 N-05 才实现关联） | 主机房A区 / 主机房B区 / 灾备机房 / 网络机房 |
| 3 | 机柜 | 非空则 `find_or_create_rack` 自动建机柜 | `A-01`…`NET-03`，共 12 个 |
| 4 | 位置(U) | `parse_u_range`：`"3U"`→(3,3)；`"1-4U"`→(1,4) | 1U/2U/3U/4U/8U 五种跨度 |
| 5 | IP地址 | `ip_addresses`，支持 `;` 分隔多 IP | 按机房网段分配 |
| 6 | 序列号 | **查重键 1**（全局唯一） | `SN-{机柜}-{序号}` |
| 7 | 资产编号 | **查重键 2**（全局唯一） | `ZC-2026-0001`…`ZC-2026-0100` |
| 8 | 使用部门 | `department` | 运维部 / 网络与安全部 / 研发中心 / 数据平台部 |
| 9 | 责任人 | `owner` | 8 个姓名轮转 |
| 10 | 功能描述 | `function` | `{角色}｜{型号}` |
| 11 | 采购日期 | 存 `NaiveDate`，接受 `YYYY-MM-DD` / `YYYY/MM/DD` / `YYYY.MM.DD` | 2022–2025 年 |
| 12 | 质保到期 | 同上 | 采购年 +3 / +5 年 |
| 13 | 状态 | `开机`/`online`→online；`离线`/`offline`→offline；**其他一切值**→unconfigured | 开机 86 / 离线 14 |
| 14 | 功率(W) | `cell_to_int`；≤0 或不可解析 → NULL | 30–3200 W |
| 15 | **设备类型** | **🔷 N-21 新增（列索引 15）**。旧版本导入**忽略此列**（`row.get(15)` → `None`，仍按 `'server'` 兜底）；新版本按此列**关联或新建型号类型**。接受中文名与英文枚举、大小写不敏感；未知值或空值 → **兜底 `other`** | 服务器 / 交换机 / 路由器 / 存储 / NAS存储 / 安全设备 / 负载均衡（见 §2 型号表） |

> **第 2 列「机房」与第 16 列「设备类型」的区别**：机房是**被丢弃**（当前版本 `excel.rs:248` 的 `let _room_name`），设备类型是**被采纳**（N-21 落地后）。前者需要 v2.0.0 的 N-05 才能生效。

## 二、数据构成

**机房 → 机柜（12 个）**

| 机房 | 机柜 | 设备数 | 占用 U | 使用率 |
|------|------|:---:|:---:|:---:|
| 主机房A区 | A-01 / A-02 / A-03 | 8 / 16 / 7 | 13 / 37 / 13 | 31% / **88%** / 31% |
| 主机房B区 | B-01 / B-02 / B-03 | 8 / 7 / 7 | 13 / 15 / 11 | 31% / 36% / 26% |
| 灾备机房 | DR-01 / DR-02 / DR-03 | 7 / 6 / 6 | 14 / 11 / 10 | 33% / 26% / 24% |
| 网络机房 | NET-01 / NET-02 / NET-03 | 10 / 9 / 9 | 11 / 10 / 11 | 26% / 24% / 26% |

> **A-02 刻意堆到 88%**，用于触发机柜视图的 `>85% 红色预警`。

**设备型号（13 个）与高度分布**

| 型号 | 类型 | 标称高度 | 功率 |
|------|------|:---:|:---:|
| Dell PowerEdge R750 | 服务器 | 2U | 750W |
| H3C UniServer R4900 G5 | 服务器 | 2U | 700W |
| 华为 FusionServer 2288H V5 | 服务器 | 2U | 650W |
| H3C UniServer B16000 | 服务器（刀片机箱） | 8U | 3200W |
| Cisco Catalyst 9300-48P | 交换机 | 1U | 200W |
| H3C S6520X-30QC-EI | 交换机 | 1U | 180W |
| 华为 NetEngine AR6280 | 路由器 | 2U | 350W |
| 华为 OceanStor 5310 V6 | 存储 | 4U | 1100W |
| Dell EMC Unity XT 480 | 存储 | 2U | 900W |
| 群晖 RS4021xs+ | NAS | 3U | 500W |
| Fortinet FortiGate 100F | 安全设备 | 1U | 150W |
| F5 BIG-IP i2600 | 负载均衡 | 1U | 250W |
| 施耐德 AP8886 机架PDU | 其他 | 1U | 30W |

**高度分布**：1U × 48、2U × 44、3U × 3、4U × 4、8U × 1

## 三、⚠️ 三个系统限制（务必先读）

### 1. 导入格式里的「设备类型」（N-21 已解决，待代码落地）

`device_models.type` 是**型号**的属性，而不是设备的属性。旧版本 `find_or_create_model`（`db/device_models.rs:98`）**硬编码 `type='server'`**：

```rust
// 旧版（v1.2.0）
"INSERT INTO device_models (name, type) VALUES (?1, 'server')"
```

**旧版后果**：13 个型号导入后**全部是"服务器"类型**，设备块配色清一色。

**N-21 的解法**：导入格式新增**第 16 列「设备类型」**，并改造 `find_or_create_model(name, type)` 在新建时使用该类型。

| 场景 | 行为 |
|------|------|
| 类型在 8 种枚举内 | **关联** —— 型号按该类型落库 |
| 类型为未知值或空 | **兜底** 为 `other`，不阻断导入，结果中告警 |
| 同名型号已存在 | **复用，且不覆盖其既有类型**（保护你在「型号管理」里手工修正的结果；重复导入保持幂等） |

**因此**：
- **N-21 代码落地后**：本目录的数据导入即可得到正确类型，**无需手工修改**
- **N-21 代码落地前**（当前状态）：第 16 列会被忽略，13 个型号仍全部落为"服务器"，需到 **设备台账 → 型号管理** 按 §2 的型号表改 10 个非服务器型号

### 2. 机柜规格**无法通过导入设置**

`find_or_create_rack`（`db/racks.rs`）同样硬编码：

```rust
"INSERT INTO racks (name, height_u) VALUES (?1, 42)"
```

**后果**：导入自动创建的机柜**一律 42U**。

**若你想测试多种机柜规格，必须先建机柜、再导入**——`find_or_create_rack` 会先按名称查找，命中则复用你建的那个（含自定义高度）。各机柜所需最低高度：

| 机柜 | 最低高度 | 建议设定 |
|------|:---:|------|
| A-02 | **37U** | 42U / 47U |
| A-01、B-01 | 17U | 42U / 24U |
| B-02 | 16U | 24U |
| A-03、DR-01 | 15U | 24U |
| B-03、DR-02、NET-01、NET-02、NET-03 | 13U | 24U |
| DR-03 | 11U | 24U |

> 例：先在界面创建 `DR-03`、`NET-02`、`NET-03` 三个 **24U** 机柜，再导入，即可同时看到 42U 与 24U 两种规格的渲染差异。

### 3. 机房列被丢弃

`excel.rs:248` 读取第 2 列后赋值给 `_room_name` 并丢弃，**导入的机柜 `room_id` 为 NULL**。
**处理**：导入后在机柜视图里手动把机柜分配到机房；或等 v2.0.0 的 N-05（导入自动关联机房）落地后再导。

## 四、验证结果

已用 Python 精确复刻 `excel.rs::import_devices_excel` 的判定逻辑（列序号读取 / `parse_u_range` / 三重查重 / `cell_to_string` 类型映射）对生成文件做回归模拟：

**主文件**

```
数据行总数 : 100      导入成功 : 100      跳过 : 0      错误 : 0
新建机柜   : 12 个    新建型号 : 13 个
功率为空   : 0 台     日期异常 : 0 台
U 位冲突   : 0 处     序列号/资产编号重复 : 0 处
```

**N-21 设备类型列（第 16 列）专项验证**

```
主文件   : 100/100 行含类型列
旧版本导入: 忽略该列 → 13 个型号全部落为 'server'
新版本导入: 13 个型号按类型列落库，兜底 other 0 次
    server 4 · switch 2 · router 1 · storage 2 · nas 1 · security 1 · loadbalancer 1 · other 1
    → 覆盖 8 种类型中的 8 种（7 类非 other 类型全覆盖）
```

**边界文件类型列用例**（覆盖 N-21 的三条分支）

| 行 | 类型列原值 | 归一化结果 | 分支 |
|:--:|-----------|-----------|------|
| 6 | `switch` | `switch` | 英文枚举关联 ✅ |
| 7 | `nas` | `nas` | 英文枚举关联 ✅ |
| 8 | `loadbalancer` | `loadbalancer` | 英文枚举关联 ✅ |
| 9 | `工业网关（未知类型值）` | `other` | **未知值兜底** ✅ |

> 主文件用的是**中文类型名**（如"交换机""存储阵列"），边界文件用的是**英文枚举**，两者都验证通过。

**边界文件**（在已导入主数据之后导入）

```
8 行拆解：
  · 3 条被三重查重拦截 = 序列号重复 + 资产编号重复 + 同机柜同名
  · 1 条被静默丢弃     = 空名称行（源码 continue，不计入统计）
  · 4 条正常导入       = 非法日期 / 单值U位 / 资源池未上架 / 空型号空字段
```

## 五、使用建议

1. **建议在全新数据库上导入主文件**。若库中已有数据，序列号/资产编号可能撞车。
2. **测查重最简方式**：把主文件**再导入一次** → 100 条应全部被跳过（`导入完成：成功 0 条，跳过重复 100 条`）。因为序列号、资产编号、同机柜同名三个键全部命中。
3. **边界文件的 4 条容错用例**会真实写库，其中 `EDGE-资源池设备-01` 与 `EDGE-未知型号-01` 落在"资源池"（未上架）；`EDGE-非法日期-01` 的日期会**静默变 NULL**（`parse_optional_date` 解析失败返回 `None`，不报错——这本身是一个可被验证的行为）。
4. **⚠️ 不要用 Excel 打开后另存再导入**。另存会把 `2025-01-01` 这类字符串转成日期类型，而 `cell_to_string` 对 `DateTime` 分支返回空串，日期会全部变 NULL。请直接使用本目录的原始文件。

## 六、重新生成

生成脚本：`.workbuddy/tmp/gen_test_data.py`（校验逻辑内建，U 位重叠/查重键冲突会直接报错退出）

```bash
python .workbuddy/tmp/gen_test_data.py
```
