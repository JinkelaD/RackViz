# RackViz 项目资料总览（整合版 · 单一事实来源）

> **整理时间**：2026-09-16（同日二次修订：N-10 收官、版本定版 2.0.0、交付基建就绪）　|　**口径**：**以当前代码 + git 历史为准**，本文快照数字仅作导航参考
> **用法**：新人 / 新 Agent 第一个要读的文件；之后按 §二 的顺序深入。

---

## 一、30 秒速读

**RackViz = 单机 Windows 桌面应用，用拖拽可视化替代 Excel 台账，管理「哪台设备在哪个机柜的哪个 U 位」。**

| 维度 | 当前状态 |
|---|---|
| 技术栈 | Tauri 2（Rust）+ React 19 + TypeScript 7 + Ant Design 6 + Vite 8 + SQLite（rusqlite bundled） |
| 版本控制 | ✅ Git（`main`，提交数与 HEAD 以 `git log` 为准；本文 §3.2 为整理时快照） |
| 对外版本号 | ✅ **2.0.0**（2026-09-16 拍板定版，四处已同步：Cargo.toml / tauri.conf.json / package.json / Layout 徽标） |
| DB schema | **v5**（v4→v5 加时间戳 + `deleted_at` + 部分唯一索引 + 6 个新索引） |
| Tauri 命令 | **34 个**（设备 9 / 机柜 5 / 机房 5 / 型号 5 / 导入导出 5 / 维护 2 / 设置 3） |
| 代码规模 | Rust 24 文件 / 5,930+ 行 / **104 个 `#[test]`**；前端 44 个 ts/tsx 文件 |
| Phase 4 功能增强 | **21 项全部收官**（2026-09-16）：19 项落地，N-16 / N-19 经主理人拍板关闭 |
| 交付基建 | ✅ README/CHANGELOG/CI workflow/pre-commit 门禁/husky 已就绪；✅ **交付形态定版（2026-09-16 主理人拍板）：绿色单文件 exe**（`bundle.targets: none`，不做 MSI/NSIS 安装包）；⏳ ESLint 暂缓（typescript-eslint 不支持 TS 7，上游 issue #10940） |

---

## 二、资料清单与阅读顺序

| 顺序 | 文件 | 性质 | 可信度 | 读它的目的 |
|:---:|------|:---:|:---:|------|
| 1 | **本文档** | 索引 + 事实基线 | ✅ 最新 | 建立全局坐标 |
| 2 | `git log --oneline` | **真实演进轨迹** | ✅ 最高 | 知道「最近在做什么、做到哪」 |
| 3 | `RackViz-v1.2-升级方案.md`（841 行） | 功能路线图（Phase 4 / v1.3 定义） | ✅ 规划有效 | 知道 N-01~N-21 分别指什么 |
| 4 | `RackViz-代码审查标准与流程.md`（597 行） | **强制开发约束** | ✅ 有效 | 每次提交前的审查清单 |
| 5 | `RackViz-v1.2-落地记录.md` | v1.2 增量实现说明 | ✅ | 理解 COALESCE/Patch\<T\> 等历史决策 |

> 注：早期文档（现状盘点-2026-09-11 / 架构分析报告 / HANDOFF / v1.1 产品介绍）已于 2026-09-16 经主理人拍板移除——结论已被本文与代码覆盖，git 历史可查。
> 内部过程文档（WORKBUDDY 新建项目提示词 / 磁盘占用与清理方案）亦于同日拍板移出仓库，不随公开发布。

**代码入口（比任何文档都可靠）**

| 想懂什么 | 直接读 |
|---|---|
| 全部后端能力 | `src-tauri/src/lib.rs:41-76`（34 个命令注册） |
| 全部数据结构 | `src-tauri/src/models.rs` |
| 全部前端调用 | `frontend/src/tauri-api.ts` |
| 领域类型单一来源 | `frontend/src/types/index.ts` |
| DB schema 与迁移链 | `src-tauri/src/migration.rs`（`CURRENT_VERSION = 5`） |
| U 位分配算法 | `frontend/src/utils/rackLayout.ts` → `findAvailableSlot()` |
| 前端数据流转 | `frontend/src/hooks/useApiList.ts` / `usePagedDevices.ts` |
| 主题预设 | `frontend/src/themes/presets.ts` |
| 撤销栈 | `frontend/src/contexts/UndoContext.tsx` + `hooks/useUndoStack.ts` |

---

## 三、事实基线（Fact Base）

### 3.1 Git 与版本

| 项 | 值 |
|---|---|
| 仓库 | 本地 `main` 分支；提交数与 HEAD 以 `git log` 为准（本文 §3.2 为整理时快照） |
| 首个提交 | `0c77fb4 chore(v2.0.0)!: Stage 1 依赖跃迁与工程基建` |
| 测试数据 | `test-data/` 4 文件已入库（100 条样例 / 边界用例 xlsx / 台账 xlsx / 报表 html） |

**版本号现状（✅ 2026-09-16 已定版并同步）**

| 位置 | 值 |
|---|---|
| `src-tauri/Cargo.toml:3` | `2.0.0` |
| `src-tauri/tauri.conf.json:4` | `2.0.0` |
| `frontend/package.json:4` | `2.0.0` |
| `frontend/src/components/Layout.tsx` 徽标 | `v2.0` |

### 3.2 提交轨迹快照（截至 2026-09-16 整理时；最新以 `git log` 为准）

| 提交 | 说明 | 对应需求 |
|---|---|---|
| `57ce44b` | 二维码安静区补至 4 模块（ISO/IEC 18004 要求，原为 2） | N-14 / 缺陷 |
| `97cd7db` | 二维码与标签加回序列号（7 字段） | N-14 |
| `e7c1534` | 二维码改 6 字段多行键值格式 | N-14 |
| `886481a` | 移除 v1 四段旧载荷兼容 | N-14 |
| `06a1a45` | 载荷扩为 5 项 + 预览不截断 + 主题精简 + 设置弹窗 token 修复 | N-14 / 缺陷 |
| `36718ec` | 设备二维码标签与打印布局（引入 `qrcode.react@4`） | **N-14** |
| `376d4bd` | 4 套主题预设（暗/亮/护眼绿/夜间蓝）+ 持久化 | **N-15** |
| `a3251ea` | 撤销栈 + 全局快捷键 + 拖拽 tooltip + 下架确认 | **N-11 / N-12 / N-13** |
| `4a2e07a` | 台账服务端分页 + 四字段搜索 + 关键词高亮 | **N-01 / N-02 / N-03** |
| `112cd6e` | 移除 `RestoreResult` 冗余 allow | 杂项 |
| `d7a8d91` | 恢复窗口按完整时长判定 + 修正 30 天边界用例 | N-09 |
| `0a138d3` | Stage 2A 对抗性验证补充用例 | 测试 |
| `634d98f` | 报表模板对齐 Rust 结构体字段（修复编译失败） | N-17 |
| `9dcb804` | HTML 报表增强 | **N-17** |
| `3ee22dc` | 数据备份 / 恢复 | **N-18** |
| `04f8f62` | Excel 导入导出增强 | **N-04 / N-05 / N-06 / N-07 / N-21** |
| `98dcc20` | 模型层扩展 + 软删/恢复 db 层 + 命令注册 | **N-09 / N-20** |
| `22a1f8c` | 类型/API 层 + 回收站 + 批量删除 + 导入向导 + 备份入口 | **N-09 / N-18 / N-20 / N-21** |
| `80e9469` | 迁移去冗余 import | 杂项 |
| `80e5874` | 接线 `migrate_v4_to_v5` | schema v5 |
| `0c58670` | `check.bat` 结束暂停，避免双击闪退 | 工程 |
| `c87f938` | 补 rooms/racks/device_models 时间戳取值，消除 E0063 | schema v5 |
| `0c77fb4` | **Stage 1：依赖跃迁与工程基建**（git 初始化 + 全部文档入库 + build/check 脚本） | 基建 |

### 3.3 技术栈（Stage 1 已大幅跃迁）

**前端**

| 类别 | 选型 | 当前版本 | 备注 |
|---|---|---|---|
| 框架 | React | **19.3.0** | 原 18.3.1 |
| 语言 | TypeScript | **7.0.2** | 原 5.6.3，`strict: true` |
| 构建 | Vite | **8.3.0** | 原 6.4.0，端口 5173 |
| UI | Ant Design | **6.6.3** | 原 5.22.0 |
| 路由 | react-router-dom | **7.18.3** | 原 6.28.0 |
| Tauri 桥接 | @tauri-apps/api | 2.11.1 | 只用 `invoke` |
| 对话框 | @tauri-apps/plugin-dialog | 2.7.3 | |
| 二维码 | **qrcode.react** | ^4.2.0 | N-14 新增 |
| ⚠️ 死依赖 | @tauri-apps/plugin-fs | ^2.5.1 | **仍在 package.json，Rust 侧已移除注册，前端无引用** |
| 图表库 | 无 | — | 机柜视图手写 DOM + CSS；报表为表格 |

**后端**

| Crate | 版本 | 用途 |
|---|---|---|
| `tauri` | 2 | 桌面框架 |
| `rusqlite` | **0.40** | SQLite（`bundled`） |
| `r2d2` / `r2d2_sqlite` | 0.8 / **0.35** | 连接池 `max_size(4)` |
| `calamine` | **0.36** | 读 xlsx |
| `rust_xlsxwriter` | **0.99** | 写 xlsx |
| `askama` | **0.16** | HTML 报表模板 |
| `chrono` / `serde` / `log` / `flexi_logger` | — | 日期 / 序列化 / 日志 |

`[profile.release]`（新增）：`opt-level = "s"` + `lto = "thin"` + `strip = "symbols"`。
`[lints.clippy]`：`unwrap_used` / `expect_used` / `panic` / `print_stdout` / `print_stderr = warn`。

### 3.4 后端能力面（**34 个** Tauri 命令）

| 分组 | 数量 | 命令 |
|---|:---:|---|
| 设备 | **9** | `list_devices`（分页）/ `list_devices_all` / `list_deleted_devices` / `restore_device` / `get_device` / `create_device` / `update_device` / `delete_device` / `delete_devices`（批量） |
| 机柜 | 5 | `list/get/create/update/delete_rack` |
| 机房 | 5 | `list/get/create/update/delete_room` |
| 型号 | 5 | `list/get/create/update/delete_device_model` |
| 导入导出 | 5 | `export_racks_excel` / `export_devices_data_excel` / `export_single_rack_excel` / `export_report_html` / `import_excel_from_path` |
| **维护**（新增） | **2** | `backup_database` / `restore_database` |
| 设置 | 3 | `get_logging_config` / `set_logging_enabled` / `open_log_dir` |

统一约定：`Result<T, AppError>`，`AppError` 序列化为 `{ code, message, detail? }`。

### 3.5 数据模型与 schema v5

```
rooms (机房)                    device_models (型号字典)
  │ 1                                  │ 1
  │ room_id  ON DELETE SET NULL        │ device_model_id  ON DELETE SET NULL
  ▼ N                                  ▼ N
racks (机柜) ──── 1:N ────► devices (设备)
  height_u (4–48U, 默认 42)     start_u / end_u / height_u
  row / col / view / sort_order  created_at / updated_at / deleted_at
```

**迁移链**

| 版本 | 内容 |
|:---:|---|
| v1 | 建 4 表 |
| v2 | 建 `settings` KV 表 |
| v3 | 7 索引 + 4 UNIQUE + 事务保护 |
| v4 | `devices.height_u` 固有高度列 + 回填 |
| **v5** | 4 表加 `created_at` / `updated_at`；`devices.deleted_at`（软删除）；**唯一索引重建为部分索引**（软删记录退出唯一约束）；新增 6 个索引 |

**关键语义**

- 外键全 `ON DELETE SET NULL` 软级联（**刻意设计：设备不丢失**）
- `devices.status`：`online` / `offline` / `unconfigured`（资源池）
- `device_models.type`：**8 种**（server / switch / router / storage / nas / security / loadbalancer / other）
- `start_u` / `end_u`：闭区间，1U 时相等，**NULL = 未上架**
- `height_u`：四级优先级 = 显式值 > U 位区间 > 型号高度 > 1
- DB 位置：`%LOCALAPPDATA%\com.rackviz.app\rackviz.db`；日志同级 `logs/`（按天轮转保留 7 天）

> 🔴 **v5 新增硬约束**：`uniq_devices_serial_no` / `uniq_devices_asset_no` 已改为
> `WHERE serial_no <> '' AND deleted_at IS NULL` 的**部分唯一索引**。
> **所有设备查询默认必须带 `deleted_at IS NULL`**，否则会把回收站里的记录查出来、也会误判重复。

### 3.6 Phase 4 功能增强：完成度

| 编号 | 功能 | 状态 | 证据 |
|:---:|---|:---:|---|
| N-01 | 服务端分页 | ✅ | `usePagedDevices.ts`、`list_devices` 分页参数 |
| N-02 | 多字段搜索 | ✅ | 四字段搜索（提交 `4a2e07a`） |
| N-03 | 搜索结果高亮 | ✅ | `utils/highlight.tsx`、`HighlightText.tsx` |
| N-04 | 导入更新模式（跳过/覆盖） | ✅ | `04f8f62` |
| N-05 | 导入关联机房 | ✅ | `04f8f62`（`_room_name` 已接线） |
| N-06 | 导入进度显示 | ✅ | `hooks/useImportProgress.ts` |
| N-07 | Excel 导出增强 | ✅ | `04f8f62` |
| N-08 | 时间戳 | ✅ | schema v5，4 表全字段 |
| N-09 | 软删除 + 30 天恢复 | ✅ | `TrashDrawer.tsx`、`restore_device` |
| N-10 | 输入验证增强（start_u≤end_u / IP / 枚举 / 边界 / 重叠） | ✅ | `f51ca2c` 后端 + `0217b6f` 前端（2026-09-16）；前后端双校验，详见 §3.6.1 |
| N-11 | 撤销（Ctrl+Z） | ✅ | `UndoContext.tsx` + `useUndoStack.ts` |
| N-12 | 键盘快捷键 | ✅ | `useGlobalShortcuts.ts` |
| N-13 | 拖拽增强（tooltip / 下架确认） | ✅ | `a3251ea` |
| N-14 | 设备二维码 / 打印标签 | ✅ | `qrcode.react` + `DeviceQrLabel.tsx`；**7 字段多行键值契约见 §3.9**；标签已改版为仅显示二维码，打印放大至 185mm 居中（`4b8d99f`） |
| N-15 | 多主题预设 | ✅ | `themes/presets.ts`（暗/亮/护眼绿/夜间蓝） |
| N-16 | 设备拓扑图 | ❌ 已关闭 | 主理人 2026-09-16 拍板：产品不需要，不引入图库依赖 |
| N-17 | 报表图表化 | ✅ | `9dcb804`（HTML 报表增强） |
| N-18 | 数据备份与恢复 | ✅ | `maintenance.rs`：backup/restore_database |
| N-19 | 多语言 i18n | ❌ 已关闭 | 主理人 2026-09-16 拍板：无多语种用户 |
| N-20 | （回收站/批量删除相关） | ✅ | `delete_devices` + `TrashDrawer` |
| N-21 | 导入向导 | ✅ | `ImportWizardModal.tsx` |

**Phase 4 收官（2026-09-16）**：21 项中 19 项落地（✅），N-16 / N-19 经主理人拍板**关闭**（不做）。含 N-10 的验证规则明细见 §3.6.1。

#### 3.6.1 N-10 输入验证增强规则明细（2026-09-16 落地）

**后端为最终防线**（`db/devices.rs` `validate_device_input`，insert/update 全路径接线，覆盖表单 / Excel 导入 / 拖拽）：

| # | 规则 | 错误类型 |
|---|---|---|
| 1 | U 位区间：end ≥ start，且任意 U 位 ≥ 1 | validation |
| 2 | U 位边界：双边齐全时须落在所属机柜 `height_u` 内（附带验机柜存在） | validation |
| 3 | **同机柜 U 位重叠互斥**：与任一其它 active 设备区间相交即冲突（排除自身/软删） | conflict |
| 4 | IP 列表：分隔符 `,，;；空白`，每项为无前导零 IPv4 或含 `:`（IPv6 放行）；允许空 | validation |
| 5 | status 枚举白名单 `online/offline/unconfigured`（空串同样拒绝） | validation |

- update 场景空间校验（规则 1-3）**仅在 U 位三要素实际变更时执行**——历史脏数据的非空间编辑（如改名）不受阻
- Excel 导入遇非法行（校验拒绝 / U 位解析失败）：**跳过该行 + 警告清单**，不整批失败（主理人拍板）
- 前端双校验（`DeviceFormModal.tsx`）：U 位上限按所选机柜动态收紧（`Form.useWatch`），切换机柜后延迟重校验；未选机柜保持宽松上限 100

### 3.7 遗留问题

| 级别 | 问题 | 说明 |
|:---:|---|---|
| 🟡 | **ESLint 依赖未安装** | typescript-eslint@8 官方不支持 TS 7（上游 issue #10940）；`npm run lint` 指向离线红线检查，`.eslintrc.cjs` 保留待上游兼容 |
| 🟡 | 死依赖 `@tauri-apps/plugin-fs` | 仍在 `frontend/package.json` |
| 🟡 | `target/` 约 3.1 GB | 新增 `[profile.release] strip=symbols` 后需全量重编才生效 |
| 🟡 | 前端 8 处 `as T` 断言、11 处 `console.error` | v1.2 遗留，未清零 |
| 🟡 | RoomTabs 删除机房无二次确认 | `RoomTabs.tsx` |
| 🟡 | 导入按列序号解析、5000 行上限静默截断 | `excel.rs`（N-21 导入向导已缓解） |
| ⚪ | `cargo test` 需完整 MSVC（vcvars）+ 无沙箱终端 | 环境限制（Q-12） |

**✅ 已解决（不要再当作待办）**

- ~~版本号四处 `1.2.0` 不一致~~ → 2026-09-16 定版 2.0.0 并四处同步
- ~~无 README.md / CHANGELOG.md~~ → 已就位（README 含安装/使用/配置/许可证说明）
- ~~无 CI~~ → `.github/workflows/ci.yml` 已建（windows-latest，与 pre-commit 门禁对齐）
- ~~`test-data/` 未入库~~ → 4 文件已入库
- ~~N-10 输入验证~~ → 2026-09-16 落地（§3.6.1）
- 项目已有 Git；`build.bat` / `check.bat` 在位且功能正常（含 MSVC 自定位）
- COALESCE 无法置空 / CSP 关闭 / LIKE 未转义 / 无索引 / 迁移无事务 —— 全部已修
- RackView 879→133 行、DeviceList 577→252 行、类型重复 12→0

### 3.8 设计约束（非 Bug，改动前必读）

| 约束 | 说明 |
|---|---|
| `dragDropEnabled: false` **不可改** | WebView 会抢走 HTML5 拖拽，上下架交互报废 |
| **查询必须带 `deleted_at IS NULL`** | v5 部分唯一索引 + 回收站语义 |
| 加迁移要改**四处** | 新 `migrate_vX_to_vY` + `CURRENT_VERSION` + `run()` match + 老分支串联 |
| 不可修改已有 `migrate_v0_to_v1` ~ `v4_to_v5` | 老库已跑过 |
| 迁移/索引须走 `with_migration_tx` | 否则失去回滚保护 |
| `start_u`/`end_u` 可为 NULL | 用 `?? 0` 兜底，**勿回退非空断言** |
| 前端类型单一来源 | 只改 `types/index.ts`，**禁止建第二套接口** |
| `racks.view` 只有 `front`/`rear` | 新建机柜默认 `front`，背面为空是预期行为 |
| release 模式隐藏控制台 | 调试看 `%LOCALAPPDATA%\com.rackviz.app\logs\` |
| AntD 6 / React 19 / TS 7 | **API 与 v5/18/5 差异大**，改 UI 前先查官方文档，勿凭旧记忆写 |

### 3.9 二维码内容契约（N-14，最终版 · 勿随意变更）

**编码格式：多行 `字段名：字段值`，恒定 7 行，顺序不可变**（冒号为全角 `：` U+FF1A）：

```
设备名称：A-01-存储阵列-01
型号：Dell EMC Unity XT 480
IP地址：10.10.1.16;10.10.1.17
资产编号：ZC-2026-0006
序列号：SN-B-02-006
使用部门：研发中心
责任人：孙鹏
```

| 约束 | 说明 |
|---|---|
| 单一数据源 | 标签正文与二维码内容**共用 `buildQrRows()`**，保证「印着的」与「扫到的」逐字一致，改一处即同步 |
| 空字段 | **仍保留整行**（如 `使用部门：`），不得省略行、不得用 `-` 占位 |
| 脏数据 | 值中的 `\r` / `\n` **必须归一化为空格**，否则一行被劈成两行、破坏行结构 |
| IP 多值 | 半角 `;` 分隔，原样输出（`Device.ip_addresses` 本就是 `;` 分隔） |
| 解析 | `parseDeviceQr` 按行序 + 字段名严格匹配，行序错乱 / 字段名未知 / 行数不符一律返回 `null` |
| 历史格式 | `RV|a|b|c` 竖线格式与 v1 四段载荷**已彻底移除，无任何兼容**（用户确认不存在已打印旧标签） |

**渲染参数与可扫性（实测）**

| 项 | 值 | 依据 |
|---|---|---|
| 纠错级别 | `M`（15%） | 打印标签需抗污损，**不降为 L** |
| 安静区 | **4 模块** | ISO/IEC 18004 规定；`qrcode.react` 的 `marginSize` 默认 0，曾误设为 2（`57ce44b` 修复） |
| 容量占用 | 198 字节 → **v10 / 53×53 模块** | UTF-8 实测；`size` 已含安静区，故安静区不影响布局 |
| 打印尺寸 | 打印媒体下 **185 × 185 mm**（`4b8d99f`：矢量放大 + 整页居中，`@page margin: 10mm`） | 屏幕预览仍 132 CSS px；SVG 无损缩放 |
| 每模块 | ≈ **3.5 mm**（打印 185mm / 53 模块） | 远高于 0.4 mm 可扫下限 |

> 📌 结论：7 字段（198B）相对 4 字段（127B，v8/45×45）仅多 1 个 QR 版本，**无需为密度砍字段**。
> 若日后新增字段，重算字节数并确认仍 ≤ v10（213B）；超过则需评估放大 `QR_SIZE` 或调整标签尺寸。

---

## 四、本地环境与命令

### 4.1 环境

| 项 | 值 |
|---|---|
| OS | Windows 10/11 x64 |
| Node | 22.22.2 |
| Rust | stable + MSVC（`%USERPROFILE%\.cargo\bin`，**默认不在 PATH**） |
| MSVC | VS2019 BuildTools 14.29.30133；脚本会自行定位 `vcvars64.bat` |
| Windows SDK | 10.0.19041.0；WebView2 Runtime 150.0.4078.105 |

> ⚠️ Git Bash 下给 cargo 传 `/c/Users/...` 会失败（MSYS 路径转换），用 `C:/Users/...`。

### 4.2 根目录脚本（双击即用）

| 脚本 | 流程 |
|---|---|
| `build.bat` | 定位 MSVC → `npm ci`（失败回退 `npm install`）→ `npm run build` → `cargo build --release` → 输出 `rackviz.exe` |
| `check.bat` | 定位 MSVC → `npm ci` → `npx tsc --noEmit` → `cargo check` → `cargo test`（结束 `pause`，不闪退） |

### 4.3 pre-commit 钩子（`.husky/pre-commit`）

```
cargo check --quiet
cargo clippy --quiet -- -D warnings
cargo test --quiet
npx tsc --noEmit --pretty
npx eslint src/ --quiet     # 未安装则跳过并告警
node scripts/lint-check.mjs # 离线红线检查
```

> ⚠️ 钩子脚本在 `.husky/`，但 git 未配置 `core.hooksPath=.husky` 时**不会自动生效**；需确认是否已安装到 `.git/hooks/`。

### 4.4 手动命令

```bash
cd src-tauri && cargo tauri dev        # 开发（首次 10-20 分钟）
cd frontend  && npm run dev            # 只跑前端（invoke 会失败）
cd frontend  && npx tsc --noEmit       # 类型检查
cd frontend  && npm run lint:offline   # 离线红线检查（ESLint 未装时的替代）
cd src-tauri && cargo build --release  # 裸 exe
cd src-tauri && cargo tauri build      # MSI + NSIS
cd src-tauri && cargo test             # 95 个用例
```

---

## 五、测试数据

| 文件 | 用途 | 入库状态 |
|---|---|---|
| `test-data/设备台账.xlsx` | 真实业务台账样本 | ❌ 未跟踪 |
| `test-data/RackViz-测试数据-100条.xlsx` | 100 条批量导入 | ✅ |
| `test-data/RackViz-测试数据-边界用例.xlsx` | 多 U / 未上架 / 重名 / 空字段 | ✅ |
| `test-data/设备报表.html` | HTML 报表导出样例 | ❌ 未跟踪 |
| `test-data/README.md` | 数据说明 | ✅ |

> 导入按**列序号 0-14** 解析，无表头识别，列顺序须严格匹配（N-21 导入向导已做映射提示）。

---

## 六、下一步建议（按 ROI）

| 优先级 | 事项 |
|:---:|---|
| 🔴 P0 | ~~拍板版本号（1.3.0 vs 2.0.0）并四处同步~~ ✅ 已定版 2.0.0（2026-09-16） |
| 🔴 P0 | ~~补 README.md + CHANGELOG.md~~ ✅ 已完成 |
| 🟡 P1 | ~~安装 ESLint 依赖 + 建 CI~~ ⚠️ CI 已建；ESLint 因 typescript-eslint 不支持 TS 7 暂缓（上游 issue #10940） |
| 🟡 P1 | ~~N-10 输入验证增强~~ ✅ 2026-09-16 完成（§3.6.1） |
| 🟡 P1 | 跑通 `cargo tauri build` 产出 MSI + NSIS（交付闭环） |
| 🟢 P2 | 清 `@tauri-apps/plugin-fs` 死依赖 |
| 🟢 P2 | ~~N-16 拓扑图~~ / ~~N-19 i18n~~ ✅ 已拍板关闭（2026-09-16） |

---

*本文档由 WorkBuddy 整理生成；未修改任何源码。事实以当前工作区代码与 git 历史为准。*
